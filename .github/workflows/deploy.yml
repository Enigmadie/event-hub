name: Deploy iot-hub

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          /home/godo/deploy/iot-hub.sh
